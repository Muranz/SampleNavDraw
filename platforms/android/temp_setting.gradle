android {
            defaultConfig {
                applicationId = "org.nativescript.SampleNavDraw"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }